---
title: Install
description: Install the Schemat CLI, one-off or globally.
---

```bash
# one-off
npx @schemat/cli dev

# or install the CLI globally
npm i -g @schemat/cli
schemat dev
```

Requires **Node >= 22** and **pnpm 9** for development from source.
